# 仓库协作约定

## 代码与提交

- 提交信息和代码注释中主要使用中文。
- 实现的代码应符合企业级标准，拒绝任何占位实现。
- 代码应简洁，需要抛出异常的地方应直接报错终止，不要引入过多 `try catch`。
- 读取和写入文件应使用 UTF-8 编码。

## 发版规则

- `main` 分支用于集成与提交，不会自动发版。
- `release` 分支启用了自动发版工作流，向该分支推送会自动执行补丁版本递增、打 tag、构建发布产物并创建 GitHub Release。
- 自动发版工作流会在当前 `style.css` 版本基础上再递增一个补丁版本，因此推送到 `release` 前必须确认该行为符合预期。
- 如需保留指定版本号，不能直接依赖 `release` 分支自动发版，必须先核对工作流行为与版本策略。
